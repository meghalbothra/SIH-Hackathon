import React from 'react';
import LoginForm from '../components/LoginPage'; // Adjust the path as needed

const LoginPage = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
